<?xml version="1.0" encoding="UTF-8"?>

<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
	 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	 xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
	 version="6.0">
    
    <session-config>
        <session-timeout>
            30
        </session-timeout>
    </session-config>
    <!-- dar de alta un servicio  -->
    <servlet>
         <!-- dar de alta un servicio   SE CAMBIO NOMBRE -->
        <servlet-name>vetcare</servlet-name>
        <servlet-class>
            <!-- utiliza las librerias actuales y tienes que revisar el nombre del paquete   -->
            org.glassfish.jersey.servlet.ServletContainer
        </servlet-class>
        <init-param>
            <!--  -->
            <param-name>jersey.config.server.provider.packages</param-name>
            <!-- paquete que contiene todo el backend -->
            <param-value>org.vetcare.rest</param-value>
            <!-- esto se cambia en cada nombre del proyecto  -->
        </init-param>
    </servlet>
    <!-- puerta de entrada al backend  -->
    <servlet-mapping>
        <!-- se enlaza la etiqueta de arriba con la etiqueta de abajo  -->
        <servlet-name>vetcare</servlet-name>
        <!--  -->
        <url-pattern>/api/*</url-pattern>
    </servlet-mapping>

    <filter>
    <filter-name>CorsFilter</filter-name>
    <filter-class>org.vetcare.rest.CorsFilter</filter-class> 
    </filter>
<filter-mapping>
    <filter-name>CorsFilter</filter-name>
    <url-pattern>/*</url-pattern>
    </filter-mapping>
</web-app>
